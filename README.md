# MEAN Stack CRUD Application

>DiscoverDollar DevOps >Technical Assignment
>Full-stack MEAN (MongoDB · >Express · Angular · Node.js) >application, containerized >with Docker, deployed via >Docker Compose on an Ubuntu >VM, automated with GitHub >Actions CI/CD, and served >through an Nginx reverse >proxy.

---

## 📌 Project Overview

This project is a full-stack CRUD application for managing Tutorials. Each tutorial has:

ID – auto-generated by MongoDB
Title – name of the tutorial
Description – brief description
Published – boolean status

**Features**

Create, Read, Update, Delete tutorials
Search tutorials by title
Fully containerized with Docker
Automated CI/CD pipeline on every git push
Deployed on a cloud Ubuntu VM accessible via port 80

---
Architecture Diagram

<image>
---

✅ Prerequisites
Make sure these are installed on your local machine before developing:

| Tool                | Install / Link                                      |
|---------------------|-----------------------------------------------------|
| Node.js             | https://nodejs.org                                  |
| Angular CLI         | npm install -g @angular/cli                         |
| Docker              | https://docs.docker.com/get-docker/                 |
| Docker Compose      | container                         |
| DockerHub                | Registry                                |
| Git                 | https://git-scm.com/                                |
| AWS Account                 | https://eu-north-1.signin.aws.amazon.com/                                |
----

**Repository Structure**

<tree image>

---

## Local Development Setup

- MongoDB running inside Docker container

- Backend (Node.js + Angular Express) running locally

 1. Clone the Repository  

 ```bash
 git clone https://github.com/altamashGit/discoverdollar-mean-devops-assignment.git 
 ```

 2. Install MongoDb using Docker:  

 ```bash
docker run -d -p 27017:27017 --name local-momgo mongo:4.0
 ```
3. Run the Backend  

```bash
cd backend
npm insall
```
```bash
node server.js
```

<image>

Result: 
Search on web browser
```bash
 http://localhost:8080
 ```
<image>

4. Run the Frontend  

```bash
cd frontend
npm install
ng serve --port 8081
```
<image>
Result: 
Search on web browser
```bash
 http://localhost:8081
 ```
 <image>

## Docker Setup  

Step 1 – Configure Environment Variables

```bash 

---

## Ec2 Instance Creation on AWS  (Ubuntu)

Step 1 – Create Ubuntu VM on AWS

AMI: Ubuntu 24.00 LTS  
Instance Type: t2.micro   
Security Group: Allow ports 22 (SSH), 80 (HTTP)  
Key pair: Download assignment.pem file  

<Ec2createiamge>

Step 2 – Install Docker on the VM  

SSH into your VM:  

```bash
ssh -i "assignment.pem" ubuntu@<VM_PUBLIC_IP>.ap-south-1.compute.amazonaws.com
```

<ssh_iamge>

Install Docker:  

```bash
# Update packages
sudo apt-get update

# Install prerequisites
sudo apt-get install -y ca-certificates curl gnupg

# Add Docker GPG key
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
  sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Add Docker repo
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
  https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Install Docker + Compose
sudo apt-get update
sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# Add ubuntu user to docker group (no sudo needed)
sudo usermod -aG docker ubuntu
newgrp docker

# Verify
docker --version
docker compose version
```
## Docker Setup
 
